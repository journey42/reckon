{
    auto_https off
}

:80 {
    encode gzip

    @backend_routes path /_event/* /_upload /ping
    handle @backend_routes {
        reverse_proxy {$API_URL}
    }

    # Reverse proxy for all other requests through the Reflex app
    handle {
        reverse_proxy {$API_URL}
    }
}
